const Loader = () => (
    <div className="fixed inset-0 flex items-center justify-center opacity-25 bg-gray-400">
        <div className="loader border-t-transparent border-solid rounded-full animate-spin border-4 border-black h-20 w-20"></div>
    </div>
);

export default Loader;
